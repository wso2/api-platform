# Test Configuration
# Enable/disable tests by setting 'enabled' to true/false
# Comment out or set to false to skip a test

tests:
  - name: TestFlagValuesUnique
    description: Verify all CLI flags have unique values
    enabled: true
  
  - name: TestGatewayBuildCommand
    description: Test gateway build command with policy manifest
    enabled: true
    # Set to false to skip this test during development
    # enabled: false
