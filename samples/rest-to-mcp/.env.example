# Ports the gateway distribution exposes.
MGMT_PORT=9090
HEALTH_PORT=9094
TRAFFIC_PORT=8443

# Host port for the generated MCP server. Change it if something on your
# machine already uses it.
MCP_PORT=5050

# How long setup.sh waits for the gateway, in 2-second attempts.
MAX_RETRIES=45
