# Appended to the distribution's configs/config.toml by setup.sh.
# Points the llm-cost policy at the model pricing file in the gateway image.

[policy_configurations.llm_cost_v1]
pricing_file = "/etc/policy-engine/llm-pricing/model_prices.json"
