{{- $unified := .Values.gateway.gatewayRuntime -}}
{{- $deployment := $unified.deployment -}}
{{- $policies := $unified.policies -}}
{{- $config := .Values.gateway.config -}}
{{- /*
  gateway-runtime's docker-entrypoint.sh defaults XDS_TLS_ENABLED to "true"
  (matching config-template.toml's own default for non-Helm deployments), but
  this chart's server.xds_tls/policy_server.tls default to "false" -- so both
  must be passed through explicitly below, or an unconfigured deployment
  crash-loops on the entrypoint's fail-closed missing-cert-paths check.
*/ -}}
{{- $xdsTLS := $config.controller.server.xds_tls -}}
{{- $policyTLS := $config.controller.policy_server.tls -}}
{{- $xdsClientCerts := $unified.xdsClientCerts | default dict -}}
{{- $xdsMTLSEnabled := or $xdsTLS.enabled $policyTLS.enabled -}}
{{- if and $xdsMTLSEnabled (not $xdsClientCerts.secretName) -}}
{{- fail "gateway.gatewayRuntime.xdsClientCerts.secretName is required when gateway.config.controller.server.xds_tls.enabled or policy_server.tls.enabled is true." -}}
{{- end -}}
{{- $trafficLogVolume := $unified.trafficLogVolume | default dict -}}
{{- $fileSinkSelected := false -}}
{{- if and $config.traffic_logging $config.traffic_logging.outputs -}}
  {{- range $config.traffic_logging.outputs -}}
    {{- if eq (lower (trim .)) "file" -}}{{- $fileSinkSelected = true -}}{{- end -}}
  {{- end -}}
{{- end -}}
{{- $trafficLogVolume = merge (dict) $trafficLogVolume (dict "mountPath" "/var/log/wso2" "sizeLimit" "512Mi") -}}
{{- $tlvEnabled := $fileSinkSelected -}}
{{- if hasKey $trafficLogVolume "enabled" -}}
  {{- $tlvEnabled = $trafficLogVolume.enabled -}}
{{- end -}}
{{- $trafficLogVolume = set $trafficLogVolume "enabled" $tlvEnabled -}}
{{- if and $trafficLogVolume.enabled $fileSinkSelected -}}
  {{- $logPath := "" -}}
  {{- if $config.traffic_logging.file -}}{{- $logPath = $config.traffic_logging.file.path | default "" -}}{{- end -}}
  {{- /*
    Normalize before comparing. hasPrefix on the raw string would accept
    "/var/log/wso2/../../etc/x.log", which shares the mount prefix textually but
    resolves outside it. `clean` collapses the traversal so the check sees the
    path the process will actually open.
  */ -}}
  {{- $logPath = clean $logPath -}}
  {{- $mount := printf "%s/" (trimSuffix "/" (clean $trafficLogVolume.mountPath)) -}}
  {{- if and $logPath (not (hasPrefix $mount $logPath)) -}}
    {{- fail (printf "gateway.config.traffic_logging.file.path (%q) is not under gateway.gatewayRuntime.trafficLogVolume.mountPath (%q), so it would not land on the mounted volume and the gateway-runtime pod would fail to start. Move the path under the mount, change the mountPath, or set trafficLogVolume.enabled=false if you are mounting your own volume." $logPath $trafficLogVolume.mountPath) -}}
  {{- end -}}
{{- end -}}
{{- if $deployment.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gateway-operator.fullname" . }}-gateway-runtime
  labels:
    {{- include "gateway-operator.componentLabels" (list . "gateway-runtime" $deployment.labels) | nindent 4 }}
  {{- with (include "gateway-operator.annotations" (list . $deployment.annotations)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  {{- if not $unified.hpa.enabled }}
  replicas: {{ $deployment.replicaCount }}
  {{- end }}
  {{- with $deployment.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "gateway-operator.componentSelectorLabels" (list . "gateway-runtime") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gateway-operator.componentPodLabels" (list . "gateway-runtime" $deployment.podLabels) | nindent 8 }}
      {{- $podAnnotations := omit (merge (dict) (default (dict) $deployment.podAnnotations) (default (dict) .Values.commonAnnotations)) "checksum/config" }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/gateway/gateway-config.yaml") . | sha256sum }}
        {{- with $podAnnotations }}
        {{- include "gateway-operator.renderStringMap" . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "gateway-operator.serviceAccountName" . }}
      {{- with (include "gateway-operator.componentImagePullSecretsBlock" (dict "root" . "componentPullSecrets" $unified.imagePullSecrets)) }}
      {{- . | nindent 6 }}
      {{- end }}
      {{- with $deployment.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $deployment.priorityClassName }}
      priorityClassName: {{ . | quote }}
      {{- end }}
      {{- with $deployment.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $deployment.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $deployment.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $deployment.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if hasKey $deployment "terminationGracePeriodSeconds" }}
      terminationGracePeriodSeconds: {{ $deployment.terminationGracePeriodSeconds }}
      {{- end }}
      {{- with $deployment.hostAliases }}
      hostAliases:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $deployment.dnsPolicy }}
      dnsPolicy: {{ . }}
      {{- end }}
      {{- with $deployment.dnsConfig }}
      dnsConfig:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if hasKey $deployment "automountServiceAccountToken" }}
      automountServiceAccountToken: {{ $deployment.automountServiceAccountToken }}
      {{- end }}
      containers:
        - name: gateway-runtime
          image: {{ include "gateway-operator.componentImage" (dict "root" . "repository" $unified.image.repository "defaultRepository" "ghcr.io/wso2/api-platform/gateway-runtime" "tag" $unified.image.tag) | quote }}
          imagePullPolicy: {{ $unified.image.pullPolicy }}
          args: ["--pol.config", "/etc/policy-engine/config.toml"]
          {{- with $deployment.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            - name: GATEWAY_CONTROLLER_HOST
              value: {{ default (printf "%s-controller" (include "gateway-operator.fullname" .)) $deployment.env.gatewayControllerHost | quote }}
            - name: LOG_LEVEL
              value: {{ default "info" $deployment.env.logLevel | quote }}
            {{- if $deployment.env.moesifKey }}
            - name: MOESIF_KEY
              value: {{ $deployment.env.moesifKey | quote }}
            {{- end }}
            - name: ROUTER_ADMIN_ENABLED
              value: {{ default false $deployment.env.routerAdminEnabled | quote }}
            - name: XDS_TLS_ENABLED
              value: {{ $xdsTLS.enabled | quote }}
            - name: POLICY_ENGINE_XDS_TLS_ENABLED
              value: {{ $policyTLS.enabled | quote }}
            {{- if $xdsTLS.enabled }}
            - name: XDS_CLIENT_CERT_PATH
              value: {{ printf "/etc/xds-certs/%s" (default "envoy-client.crt" $xdsClientCerts.routerCertKey) }}
            - name: XDS_CLIENT_KEY_PATH
              value: {{ printf "/etc/xds-certs/%s" (default "envoy-client.key" $xdsClientCerts.routerKeyKey) }}
            - name: XDS_CLIENT_CA_PATH
              value: {{ printf "/etc/xds-certs/%s" (default "ca.crt" $xdsClientCerts.caCertKey) }}
            {{- end }}
            {{- if $policyTLS.enabled }}
            - name: POLICY_ENGINE_XDS_CLIENT_CERT_PATH
              value: {{ printf "/etc/xds-certs/%s" (default "policy-engine-client.crt" $xdsClientCerts.policyEngineCertKey) }}
            - name: POLICY_ENGINE_XDS_CLIENT_KEY_PATH
              value: {{ printf "/etc/xds-certs/%s" (default "policy-engine-client.key" $xdsClientCerts.policyEngineKeyKey) }}
            - name: POLICY_ENGINE_XDS_CLIENT_CA_PATH
              value: {{ printf "/etc/xds-certs/%s" (default "ca.crt" $xdsClientCerts.caCertKey) }}
            {{- end }}
            {{- range $deployment.extraEnv }}
            - {{- toYaml . | nindent 14 }}
            {{- end }}
          {{- with $deployment.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ $unified.service.ports.http }}
              protocol: TCP
            - name: https
              containerPort: {{ $unified.service.ports.https }}
              protocol: TCP
            - name: router-admin
              containerPort: {{ $unified.service.ports.routerAdmin }}
              protocol: TCP
            - name: pe-admin
              containerPort: {{ $unified.service.ports.policyEngineAdmin }}
              protocol: TCP
            - name: pe-metrics
              containerPort: {{ $unified.service.ports.policyEngineMetrics }}
              protocol: TCP
          {{- /*
          Health-check probes must dial the router's actual bound port/scheme,
          sourced directly from gateway.config.router (listener_port/https_port/
          https_enabled) — the same values the router itself binds to in
          gateway-config.yaml. They must NOT be derived from the "http"/"https"
          named container ports below: those come from the independently
          configurable gatewayRuntime.service.ports.*, which nothing forces to
          stay in sync with gateway.config.router's actual bind ports, and a
          Kubernetes httpGet probe's "port" is a raw dial target that need not
          match any declared containerPort anyway.
          */}}
          {{- $httpsEnabled := $config.router.https_enabled }}
          {{- $healthPort := $config.router.listener_port }}
          {{- $healthScheme := "HTTP" }}
          {{- if $httpsEnabled }}
            {{- $healthPort = $config.router.https_port }}
            {{- $healthScheme = "HTTPS" }}
          {{- end }}
          {{- $livenessProbe := deepCopy $deployment.livenessProbe }}
          {{- if hasKey $livenessProbe "httpGet" }}
            {{- $_ := set $livenessProbe.httpGet "port" $healthPort }}
            {{- $_ := set $livenessProbe.httpGet "scheme" $healthScheme }}
          {{- end }}
          livenessProbe:
            {{- toYaml $livenessProbe | nindent 12 }}
          {{- with $deployment.startupProbe }}
          {{- $startupProbe := deepCopy . }}
          {{- if hasKey $startupProbe "httpGet" }}
            {{- $_ := set $startupProbe.httpGet "port" $healthPort }}
            {{- $_ := set $startupProbe.httpGet "scheme" $healthScheme }}
          {{- end }}
          startupProbe:
            {{- toYaml $startupProbe | nindent 12 }}
          {{- end }}
          {{- $readinessProbe := deepCopy $deployment.readinessProbe }}
          {{- if hasKey $readinessProbe "httpGet" }}
            {{- $_ := set $readinessProbe.httpGet "port" $healthPort }}
            {{- $_ := set $readinessProbe.httpGet "scheme" $healthScheme }}
          {{- end }}
          readinessProbe:
            {{- toYaml $readinessProbe | nindent 12 }}
          resources:
            {{- toYaml ($deployment.resources | default dict) | nindent 12 }}
          volumeMounts:
            - name: gateway-config
              mountPath: /etc/policy-engine/config.toml
              subPath: config.toml
            {{- if $policies.llmPricing.enabled }}
            - name: llm-pricing
              mountPath: /etc/policy-engine/llm-pricing/model_prices.json
              subPath: model_prices.json
              readOnly: true
            {{- end }}
            {{- if $trafficLogVolume.enabled }}
            - name: traffic-log
              mountPath: {{ $trafficLogVolume.mountPath | quote }}
            {{- end }}
            {{- if $xdsMTLSEnabled }}
            - name: xds-client-certs
              mountPath: /etc/xds-certs
              readOnly: true
            {{- end }}
            {{- with $deployment.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: gateway-config
          configMap:
            name: {{ include "gateway-operator.fullname" . }}-config
        {{- if $policies.llmPricing.enabled }}
        - name: llm-pricing
          configMap:
            name: {{ default (printf "%s-llm-pricing" (include "gateway-operator.fullname" .)) $policies.llmPricing.configMapName }}
        {{- end }}
        {{- if $trafficLogVolume.enabled }}
        {{- /*
        Writable volume for the traffic_logging "file" sink.
        request and response bodies written here are invisible
        to `kubectl logs` and to any DaemonSet or host forwarder shipping
        container logs onward.
        sizeLimit must stay comfortably ABOVE 2 x traffic_logging.file.max_size_mb
        */}}
        - name: traffic-log
          emptyDir:
            {{- if $trafficLogVolume.sizeLimit }}
            sizeLimit: {{ $trafficLogVolume.sizeLimit }}
            {{- end }}
            {{- if $trafficLogVolume.medium }}
            medium: {{ $trafficLogVolume.medium | quote }}
            {{- end }}
        {{- end }}
        {{- if $xdsMTLSEnabled }}
        - name: xds-client-certs
          secret:
            secretName: {{ $xdsClientCerts.secretName }}
        {{- end }}
        {{- with $deployment.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
{{- end }}

