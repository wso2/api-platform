{{- $controller := .Values.gateway.controller -}}
{{- $deployment := $controller.deployment -}}
{{- if and (not .Values.gateway.developmentMode) $deployment.enabled }}
{{- if not $controller.encryptionKeys.enabled }}
{{- fail "gateway.controller.encryptionKeys must be enabled when gateway.developmentMode is false. Either set gateway.developmentMode=true or configure gateway.controller.encryptionKeys with a Kubernetes secret containing the AES-GCM key." }}
{{- end }}
{{- end }}
{{- if $deployment.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gateway-operator.fullname" . }}-controller
  labels:
    {{- include "gateway-operator.componentLabels" (list . "controller" $deployment.labels) | nindent 4 }}
  {{- with (include "gateway-operator.annotations" (list . $deployment.annotations)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  {{- if not $controller.hpa.enabled }}
  replicas: {{ $deployment.replicaCount }}
  {{- end }}
  {{- with $deployment.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "gateway-operator.componentSelectorLabels" (list . "controller") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gateway-operator.componentPodLabels" (list . "controller" $deployment.podLabels) | nindent 8 }}
      {{- $podAnnotations := omit (merge (dict) (default (dict) $deployment.podAnnotations) (default (dict) .Values.commonAnnotations)) "checksum/config" }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/gateway/gateway-config.yaml") . | sha256sum }}
        {{- with $podAnnotations }}
        {{- include "gateway-operator.renderStringMap" . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "gateway-operator.serviceAccountName" . }}
      {{- with (include "gateway-operator.componentImagePullSecretsBlock" (dict "root" . "componentPullSecrets" $controller.imagePullSecrets)) }}
      {{- . | nindent 6 }}
      {{- end }}
      {{- with $deployment.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $deployment.priorityClassName }}
      priorityClassName: {{ . | quote }}
      {{- end }}
      {{- with $deployment.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $deployment.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $deployment.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $deployment.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if hasKey $deployment "terminationGracePeriodSeconds" }}
      terminationGracePeriodSeconds: {{ $deployment.terminationGracePeriodSeconds }}
      {{- end }}
      {{- with $deployment.hostAliases }}
      hostAliases:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $deployment.dnsPolicy }}
      dnsPolicy: {{ . }}
      {{- end }}
      {{- with $deployment.dnsConfig }}
      dnsConfig:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if hasKey $deployment "automountServiceAccountToken" }}
      automountServiceAccountToken: {{ $deployment.automountServiceAccountToken }}
      {{- end }}
      containers:
        - name: gateway-controller
          image: {{ include "gateway-operator.componentImage" (dict "root" . "repository" $controller.image.repository "defaultRepository" "ghcr.io/wso2/api-platform/gateway-controller" "tag" $controller.image.tag) | quote }}
          imagePullPolicy: {{ $controller.image.pullPolicy }}
          {{- with $deployment.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          args:
            - "-config"
            - "/etc/gateway-controller/config.toml"
          env:
            - name: APIP_GW_CONTROLPLANE_HOST
              value: {{ $controller.controlPlane.host | quote }}
            - name: APIP_GW_GATEWAY_REGISTRATION_TOKEN
              {{- if $controller.controlPlane.token.secretName }}
              valueFrom:
                secretKeyRef:
                  name: {{ $controller.controlPlane.token.secretName }}
                  key: {{ $controller.controlPlane.token.key | default "token" }}
              {{- else }}
              value: {{ $controller.controlPlane.token.value | quote }}
              {{- end }}
            - name: APIP_GW_CONTROLLER_LOGGING_LEVEL
              value: {{ $controller.logging.level | quote }}
            - name: APIP_GW_CONTROLLER_STORAGE_TYPE
              value: {{ $controller.storage.type | quote }}
            - name: APIP_GW_CONTROLLER_STORAGE_SQLITE_PATH
              value: {{ $controller.storage.sqlitePath | quote }}
            - name: GATEWAY_CONTROLLER_HOST
              value: {{ default (printf "%s-controller" (include "gateway-operator.fullname" .)) $deployment.env.gatewayControllerHost | quote }}
            - name: APIP_GW_CONTROLLER_METRICS_PORT
              value: {{ printf "%d" (int $controller.metrics.port) | quote }}
            - name: APIP_GW_DEVELOPMENT_MODE
              value: {{ .Values.gateway.developmentMode | toString | quote }}
            {{- if and (eq .Values.gateway.config.controller.storage.type "postgres") $controller.postgres.passwordSecretRef.name }}
            - name: APIP_GW_CONTROLLER_STORAGE_POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $controller.postgres.passwordSecretRef.name }}
                  key: {{ $controller.postgres.passwordSecretRef.key | default "password" }}
            {{- end }}
            {{- if and (eq .Values.gateway.config.controller.storage.type "sqlserver") $controller.sqlserver.passwordSecretRef.name }}
            - name: APIP_GW_CONTROLLER_STORAGE_DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ $controller.sqlserver.passwordSecretRef.name }}
                  key: {{ $controller.sqlserver.passwordSecretRef.key | default "password" }}
            {{- end }}
            {{- range $deployment.extraEnv }}
            - {{- toYaml . | nindent 14 }}
            {{- end }}
            {{- with $deployment.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
            {{- end }}
          ports:
            - name: rest
              containerPort: {{ $controller.service.ports.rest }}
              protocol: TCP
            - name: xds
              containerPort: {{ $controller.service.ports.xds }}
              protocol: TCP
            - name: policy
              containerPort: {{ $controller.service.ports.policy }}
              protocol: TCP
            - name: admin
              containerPort: {{ $controller.service.ports.admin }}
              protocol: TCP
            - name: metrics
              containerPort: {{ $controller.service.ports.metrics }}
              protocol: TCP
          livenessProbe:
            {{- toYaml $deployment.livenessProbe | nindent 12 }}
          {{- with $deployment.startupProbe }}
          startupProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          readinessProbe:
            {{- toYaml $deployment.readinessProbe | nindent 12 }}
          resources:
            {{- toYaml ($deployment.resources | default dict) | nindent 12 }}
          volumeMounts:
            - name: controller-data
              mountPath: {{ $deployment.volumeMountPath }}
            - name: controller-config
              mountPath: /etc/gateway-controller/config.toml
              subPath: config.toml
            {{- if $controller.tls.enabled }}
            - name: listener-certs
              mountPath: /app/listener-certs
              readOnly: true
            {{- end }}
            {{- if $controller.upstreamCerts.enabled }}
            {{- if $controller.upstreamCerts.secretName }}
            - name: upstream-certs
              mountPath: /app/certificates
              readOnly: true
            {{- else if $controller.upstreamCerts.configMapName }}
            - name: upstream-certs
              mountPath: /app/certificates
              readOnly: true
            {{- end }}
            {{- end }}
            {{- if $controller.encryptionKeys.enabled }}
            - name: encryption-keys
              mountPath: {{ $controller.encryptionKeys.mountPath }}
              readOnly: true
            {{- end }}
            {{- with $deployment.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
      volumes:
        - name: controller-data
          {{- if $controller.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ if $controller.persistence.existingClaim }}{{ $controller.persistence.existingClaim }}{{ else }}{{ include "gateway-operator.fullname" . }}-controller-data{{ end }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: controller-config
          configMap:
            name: {{ include "gateway-operator.fullname" . }}-config
            items:
              - key: config.toml
                path: config.toml
        {{- if $controller.tls.enabled }}
        - name: listener-certs
          secret:
            {{- if eq $controller.tls.certificateProvider "cert-manager" }}
            secretName: {{ include "gateway-operator.fullname" . }}-controller-tls
            {{- else if eq $controller.tls.certificateProvider "secret" }}
            secretName: {{ $controller.tls.secret.name }}
            {{- end }}
            items:
              - key: {{ if eq $controller.tls.certificateProvider "cert-manager" }}tls.crt{{ else }}{{ $controller.tls.secret.certKey }}{{ end }}
                path: default-listener.crt
              - key: {{ if eq $controller.tls.certificateProvider "cert-manager" }}tls.key{{ else }}{{ $controller.tls.secret.keyKey }}{{ end }}
                path: default-listener.key
        {{- end }}
        {{- if $controller.upstreamCerts.enabled }}
        {{- if $controller.upstreamCerts.secretName }}
        - name: upstream-certs
          secret:
            secretName: {{ $controller.upstreamCerts.secretName }}
        {{- else if $controller.upstreamCerts.configMapName }}
        - name: upstream-certs
          configMap:
            name: {{ $controller.upstreamCerts.configMapName }}
        {{- end }}
        {{- end }}
        {{- if $controller.encryptionKeys.enabled }}
        - name: encryption-keys
          secret:
            secretName: {{ $controller.encryptionKeys.secretName }}
        {{- end }}
        {{- with $deployment.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
{{- end }}
