{{- $controller := .Values.gateway.controller -}}
{{- $persistence := $controller.persistence -}}
{{- if and $controller.deployment.enabled $persistence.enabled (not $persistence.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "gateway-operator.fullname" . }}-controller-data
  labels:
    {{- include "gateway-operator.componentLabels" (list . "controller" $persistence.labels) | nindent 4 }}
  {{- with (include "gateway-operator.annotations" (list . $persistence.annotations)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  accessModes:
    {{- range $persistence.accessModes }}
    - {{ . }}
    {{- end }}
  resources:
    requests:
      storage: {{ $persistence.size }}
  {{- if $persistence.storageClass }}
  storageClassName: {{ $persistence.storageClass | quote }}
  {{- end }}
{{- end }}
