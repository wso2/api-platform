---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: apiconfigurations.api.api-platform.wso2.com
spec:
  group: api.api-platform.wso2.com
  names:
    kind: APIConfiguration
    listKind: APIConfigurationList
    plural: apiconfigurations
    singular: apiconfiguration
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: APIConfiguration is the Schema for the apiconfigurations API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: APIConfigurationSpec defines the desired state of APIConfiguration
            properties:
              apiConfiguration:
                description: APIConfiguration contains the API configuration from
                  the gateway controller
                properties:
                  data:
                    description: APIConfigData defines model for APIConfigData.
                    properties:
                      context:
                        description: Context Base path for all API routes (must start
                          with /, no trailing slash)
                        type: string
                      name:
                        description: Name Human-readable API name (must be URL-friendly
                          - only letters, numbers, spaces, hyphens, underscores, and
                          dots allowed)
                        type: string
                      operations:
                        description: Operations List of HTTP operations/routes
                        items:
                          description: Operation defines model for Operation.
                          properties:
                            method:
                              description: Method HTTP method
                              type: string
                            path:
                              description: Path Route path with optional {param} placeholders
                              type: string
                          required:
                          - method
                          - path
                          type: object
                        type: array
                      upstream:
                        description: Upstream List of backend service URLs
                        items:
                          description: Upstream defines model for Upstream.
                          properties:
                            url:
                              description: Url Backend service URL (may include path
                                prefix like /api/v2)
                              type: string
                          required:
                          - url
                          type: object
                        type: array
                      version:
                        description: Version Semantic version of the API
                        type: string
                    required:
                    - context
                    - name
                    - operations
                    - upstream
                    - version
                    type: object
                  kind:
                    description: Kind API type
                    type: string
                  version:
                    description: Version API specification version
                    type: string
                required:
                - data
                - kind
                - version
                type: object
              gatewayRefs:
                description: |-
                  GatewayRefs is a list of explicit references to GatewayConfigurations.
                  If not specified, the API will be selected by gateways based on their APISelector.
                  If specified, the API will be deployed only to the referenced gateways.
                items:
                  description: GatewayReference defines a reference to a GatewayConfiguration
                  properties:
                    name:
                      description: Name is the name of the GatewayConfiguration
                      type: string
                    namespace:
                      description: |-
                        Namespace is the namespace of the GatewayConfiguration
                        If empty, the API's namespace is used
                      type: string
                  required:
                  - name
                  type: object
                type: array
            type: object
          status:
            description: APIConfigurationStatus defines the observed state of APIConfiguration
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the API's state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              deployedGateways:
                description: DeployedGateways is a list of gateways where this API
                  is deployed
                items:
                  type: string
                type: array
              lastUpdateTime:
                description: LastUpdateTime is the last time the status was updated
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed spec
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the API (Pending,
                  Deployed, Failed)
                enum:
                - Pending
                - Deployed
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
