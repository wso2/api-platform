package: api
output: api/generated_eventgateway.go
generate:
  models: true
output-options:
  yaml-tags: true
  # Generate every schema under components.schemas, not only those reachable from an operation.
  skip-prune: true
