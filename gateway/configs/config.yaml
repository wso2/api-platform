gateway_controller:
  # Authentication - admin user required for login
  auth:
    basic:
      users:
        - username: "admin"
          password: "admin"
          password_hashed: false
          roles: ["admin"]

