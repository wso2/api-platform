<!-- ── Hero ───────────────────────────────────────────────── -->
<section class="hh-hero" aria-label="Portal hero">
  <div class="hh-particles" id="hh-particles" aria-hidden="true"></div>

  <div class="hh-hero-body">
    <span class="hh-pill">
      <i class="bi bi-stars" aria-hidden="true"></i>
      Ready for AI agents
    </span>

    <h1 class="hh-headline">
      {{#if (eq devportalMode "MCPSERVERSONLY")}}
      <span class="hh-headline-accent">MCP servers</span><br>built for AI agents.
      {{else if (eq devportalMode "APISONLY")}}
      <span class="hh-headline-accent">APIs &amp; workflows</span><br>built for developers and AI.
      {{else}}
      <span class="hh-headline-accent">APIs, MCP servers &amp; workflows</span><br>built for developers and AI.
      {{/if}}
    </h1>

    <p class="hh-desc">
      {{#if (eq devportalMode "MCPSERVERSONLY")}}
      Discover MCP tools and servers, configure them for your AI agents, and get up and running in minutes.
      {{else}}
      Discover, connect, and orchestrate APIs, MCP tools, and workflows. Browse the catalogs, subscribe, and get going in minutes.
      {{/if}}
    </p>

    <div class="hh-ctas">
      {{#in devportalMode values="DEFAULT,APISONLY"}}
      <a href="{{baseUrl}}/apis" class="hh-btn hh-btn--gradient">
        Browse our APIs <i class="bi bi-arrow-right" aria-hidden="true"></i>
      </a>
      {{/in}}
      {{#in devportalMode values="DEFAULT,MCPSERVERSONLY"}}
      <a href="{{baseUrl}}/mcps" class="hh-btn hh-btn--gradient">
        Browse our MCP servers <i class="bi bi-arrow-right" aria-hidden="true"></i>
      </a>
      {{/in}}
      {{#if showApiWorkflowsNav}}
      <button type="button" class="hh-btn hh-btn--ai" data-bs-toggle="modal" data-bs-target="#discoverAiModal">
        <i class="bi bi-stars" aria-hidden="true"></i> Discover with AI
      </button>
      {{/if}}
    </div>
  </div>
</section>

<!-- ── Discover with AI modal ────────────────────────────── -->
{{#if showApiWorkflowsNav}}
<div class="modal fade" id="discoverAiModal" tabindex="-1" aria-labelledby="discoverAiModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg modal-dialog-scrollable modal-dialog-centered">
    <div class="modal-content hh-modal">

      <!-- Modal header -->
      <div class="hh-modal-header">
        <div class="hh-modal-title-row">
          <span class="hh-modal-icon"><i class="bi bi-stars" aria-hidden="true"></i></span>
          <h2 class="hh-modal-title" id="discoverAiModalLabel">Discover with AI agents</h2>
        </div>
        <button type="button" class="hh-modal-close" data-bs-dismiss="modal" aria-label="Close">
          <i class="bi bi-x-lg" aria-hidden="true"></i>
        </button>
      </div>

      <!-- Info bar -->
      <div class="hh-modal-info">
        <p class="hh-modal-info-text">Use this prompt to let your AI agent explore and discover what's available in this portal.</p>
        <button type="button" class="hh-run-btn" id="discoverBtnRunClaude">
          <i class="bi bi-play-fill" aria-hidden="true"></i> Run in Claude
        </button>
      </div>

      <!-- Prompt body -->
      <div class="modal-body hh-modal-body">
        <div class="hh-prompt-card">
          <div class="hh-prompt-actions">
            <button type="button" class="hh-prompt-tool" id="discoverBtnDownloadPrompt" title="Download as .txt">
              <i class="bi bi-download"></i>
            </button>
            <button type="button" class="hh-prompt-tool" id="discoverBtnCopyPrompt" title="Copy prompt">
              <i class="bi bi-copy" id="discoverCopyIcon"></i>
            </button>
          </div>
          <pre class="hh-prompt-pre" id="discoverPromptText"></pre>
        </div>
      </div>

    </div>
  </div>
</div>
{{/if}}

<script>
/* Particles init — runs after DOM ready */
(function () {
  function init() {
    if (typeof window.particlesJS === 'undefined') return;
    var el = document.getElementById('hh-particles');
    if (!el) return;
    window.particlesJS('hh-particles', {
      particles: {
        number: { value: 64, density: { enable: true, value_area: 900 } },
        color: { value: ['#ffffff', '#f9a04b', '#5cd1ff'] },
        shape: { type: 'circle' },
        opacity: { value: 0.5, random: true },
        size: { value: 2.6, random: true },
        line_linked: { enable: true, distance: 150, color: '#9fb6cc', opacity: 0.18, width: 1 },
        move: { enable: true, speed: 1.1, direction: 'none', out_mode: 'out' },
      },
      interactivity: {
        detect_on: 'canvas',
        events: { onhover: { enable: true, mode: 'grab' }, resize: true },
        modes: { grab: { distance: 160, line_linked: { opacity: 0.35 } } },
      },
      retina_detect: true,
    });
  }
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init);
  } else {
    init();
  }
  /* Retry once after a short delay in case particles.js loads after this script */
  setTimeout(init, 400);
})();
</script>
