worker_processes  1;
pid        /tmp/nginx.pid;

events { worker_connections 1024; }
http {
    include    mime.types;
    sendfile on;

    # Define loggable variable for conditional logging
    map $request_uri $loggable {
        default 1;
    }

    # Use tmp locations under /tmp which are writable in container
    client_body_temp_path /tmp/client_temp;
    proxy_temp_path /tmp/proxy_temp_path;
    fastcgi_temp_path /tmp/fastcgi_temp;
    uwsgi_temp_path /tmp/uwsgi_temp;
    scgi_temp_path /tmp/scgi_temp;

    server {
        gzip on;
        gzip_types text/plain text/css image/svg+xml image/png application/javascript application/json application/zip font/woff font/woff2;
        root /app/;
        index index.html;
        server_name localhost;
        listen 8080;
        try_files $uri /index.html;

        location /index.html {
            add_header 'Cache-Control' 'no-store';

            # Security Headers
            add_header X-Frame-Options "sameorigin" always;
            add_header Content-Security-Policy "frame-ancestors 'self'" always;
            add_header X-Content-Type-Options "nosniff" always;
            add_header X-Permitted-Cross-Domain-Policies "none" always;
            add_header X-XSS-Protection "1; mode=block" always;
        }

        # Serve runtime config from a writable tmp location
        location = /runtime-config.js {
            default_type application/javascript;
            alias /tmp/runtime-config.js;
            add_header 'Cache-Control' 'no-store';
            access_log off;
        }

        location / {
            try_files $uri $uri/ /index.html;
        }

        # Write access logs under /tmp so non-root user can write at runtime
        access_log /tmp/nginx/logs/access.log combined if=$loggable;
    }
}
