/*
 * Production common config for the API Platform (Oxygen) console.
 *
 * index.html loads this alongside api-platform.env.config.js. All runtime
 * configuration for the deployed console is supplied via environment variables
 * (api-platform.env.config.js, rendered at startup), so this file is
 * intentionally empty — it exists only so the <script> tag resolves.
 */
