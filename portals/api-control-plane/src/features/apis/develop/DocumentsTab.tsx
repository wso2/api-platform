import { EmptyState } from '../../../components/StateViews';

export function DocumentsTab() {
  return (
    <EmptyState
      title="Documentation not available yet"
      description="API documentation will be available here once the platform supports it."
    />
  );
}
