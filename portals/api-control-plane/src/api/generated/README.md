# Generated API Clients

OpenAPI `typescript-axios` clients for the API Platform live here.

This directory is intentionally kept app-local so the replacement console does
not import legacy `choreo-console/src/data/api` hooks or generated types. Add
the MVP backend specs here as they are finalized, then generate clients into
subdirectories such as `src/api/generated/ProjectApi`.
