name: Gateway Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 0.1.0)'
        required: true
        type: string

env:
  DOCKER_REGISTRY: ghcr.io/renuka-fernando/api-platform

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set version
        id: version
        run: |
          VERSION=${{ inputs.version }}
          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "Releasing version: ${VERSION}"
          echo "${VERSION}" > gateway/VERSION
          echo "Updated gateway/VERSION to ${VERSION}"

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Run tests
        run: make test-gateway

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.API_PLATFORM_BOT_TOKEN }}

      - name: Build and push multi arch Docker images
        run: make build-and-push-gateway-multiarch

      - name: Create GitHub Release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.API_PLATFORM_BOT_TOKEN }}
        with:
          tag_name: gateway-v${{ steps.version.outputs.version }}
          release_name: Gateway ${{ steps.version.outputs.version }}
          body: |
            ## Gateway Release ${{ steps.version.outputs.version }}

            ### Docker Images
            - `${{ env.DOCKER_REGISTRY }}/gateway-controller:${{ steps.version.outputs.version }}`
            - `${{ env.DOCKER_REGISTRY }}/policy-engine:${{ steps.version.outputs.version }}`
            - `${{ env.DOCKER_REGISTRY }}/gateway-builder:${{ steps.version.outputs.version }}`
            - `${{ env.DOCKER_REGISTRY }}/gateway-router:${{ steps.version.outputs.version }}`
          draft: true
          prerelease: ${{ contains(steps.version.outputs.version, '-') }}
